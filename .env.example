# Optional runtime variables
# Path to local tesseract binary if not in PATH
TESSERACT_CMD=

# Optional Hugging Face token for faster model downloads/rate limits
HF_TOKEN=

# Optional DB overrides if used in your local deployment
MONGODB_URI=mongodb://localhost:27017/
DB_NAME=smart_eval
